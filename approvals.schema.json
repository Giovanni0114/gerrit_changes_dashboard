{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Gerrit Approvals Dashboard Config",
    "description": "Configuration file for gerrit_approvals_config.py",
    "type": "object",
    "properties": {
        "interval": {
            "type": "integer",
            "description": "Refresh interval in seconds",
            "default": 30,
            "minimum": 1
        },
        "default_host": {
            "type": "string",
            "description": "Default host to be used for changes"
        },
        "default_port": {
            "type": "integer",
            "description": "Default SSH port to be used for changes",
            "minimum": 1,
            "maximum": 65535
        },
        "email": {
            "type": "string",
            "description": "Owner email for fetching open changes. Falls back to git config user.email."
        },
        "changes": {
            "type": "array",
            "description": "List of Gerrit changes to track",
            "items": {
                "type": "object",
                "properties": {
                    "host": {
                        "type": "string",
                        "description": "Gerrit SSH host"
                    },
                    "hash": {
                        "type": "string",
                        "description": "Git commit hash or Gerrit change-id",
                        "minLength": 1
                    },
                    "waiting": {
                        "type": "boolean",
                        "description": "When true, row is dimmed until an approval changes, then auto-set to false",
                        "default": false
                    },
                    "disabled": {
                        "type": "boolean",
                        "description": "When true, change is skipped during SSH queries and visually marked as disabled",
                        "default": false
                    },
                    "port": {
                        "type": "integer",
                        "description": "SSH port for this change, overrides default_port",
                        "minimum": 1,
                        "maximum": 65535
                    }
                },
                "required": [
                    "hash"
                ],
                "additionalProperties": false
            },
            "minItems": 1
        }
    },
    "required": [
        "changes"
    ],
    "additionalProperties": false
}
